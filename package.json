{
    "scripts": {
        "dev": "vitepress dev",
        "build": "vitepress build",
        "preview": "vitepress preview",
        "updateNav": "node generate.cjs && node countFiles.mjs"
    },
    "devDependencies": {
        "vitepress": "^1.5.0"
    }
}
