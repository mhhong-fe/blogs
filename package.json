{
    "scripts": {
        "dev": "vitepress dev",
        "predev": "pnpm updateNav",
        "build": "vitepress build",
        "prebuild": "pnpm updateNav",
        "preview": "vitepress preview",
        "updateNav": "node generate.cjs && node countFiles.mjs",
        "watch:docs": "node scripts/watch.mjs",
        "setup:hooks": "node scripts/setup-hooks.mjs",
        "postinstall": "node scripts/setup-hooks.mjs || true"
    },
    "devDependencies": {
        "chokidar": "^3.6.0",
        "vitepress": "^1.5.0"
    }
}
