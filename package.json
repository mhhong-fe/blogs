{
    "scripts": {
        "dev": "vitepress dev",
        "build": "node generate.cjs && node countFile.mjs && vitepress build",
        "preview": "vitepress preview",
        "updateNav": "node generate.cjs"
    },
    "devDependencies": {
        "vitepress": "^1.5.0"
    }
}
