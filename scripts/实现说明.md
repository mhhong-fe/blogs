# 自动化方案实现过程详解

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                  自动化更新流程                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  触发方式                                                │
│  ├─ Git Pre-commit Hook  ──┐                          │
│  ├─ npm scripts (predev/prebuild) ──┼─→ updateNav ──┐ │
│  └─ 文件监听 (watch.mjs) ───┘                          │ │
│                                                         │ │
│                                                         │ │
│  核心脚本                                                │ │
│  ├─ generate.cjs ────────────┐                        │ │
│  └─ countFiles.mjs ───────────┼─→ 更新配置文件          │ │
│                                                         │ │
│  输出文件                                                │ │
│  ├─ config.js (导航配置) ─────┘                        │ │
│  └─ docs/index.md (首页统计) ──┘                       │ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 核心脚本详解

### 1. generate.cjs - 导航配置生成器

**作用**：扫描 `docs` 目录结构，自动生成导航（nav）和侧边栏（sidebar）配置。

**工作流程**：

```javascript
// 步骤1: 扫描一级目录，生成导航
generateNav(docsPath)
  ↓
  遍历 docs 下的所有目录
  ↓
  对每个目录调用 getFirstFileLink() 找到第一个 .md 文件
  ↓
  生成 nav 数组: [{ text: "前端基础", link: "/前端基础/..." }, ...]

// 步骤2: 为每个导航项生成侧边栏
nav.forEach(navItem => {
  generateSidebar(目录路径)
    ↓
    递归遍历目录下的所有文件和子目录
    ↓
    生成嵌套的 sidebar 结构
})

// 步骤3: 排序导航项
getSortedNav(nav)
  ↓
  按照预定义的顺序重新排列

// 步骤4: 写入 config.js
fs.writeFileSync(config.js, JSON.stringify({ nav, sidebar }))
```

**关键函数**：

1. **`getFirstFileLink(dirPath)`**
   - 递归查找目录下的第一个 `.md` 文件
   - 返回相对路径（去掉 `.md` 扩展名）
   - 用于设置导航链接

2. **`generateSidebar(dirPath, basePath)`**
   - 递归处理目录结构
   - 目录 → `{ text, collapsed: true, items: [...] }`
   - 文件 → `{ text, link: "..." }`
   - 支持多级嵌套

3. **`getSortedNav(navList)`**
   - 按照预定义顺序排序导航项
   - 确保导航顺序一致

**输出示例**：
```javascript
// config.js
module.exports = {
  nav: [
    { text: "前端基础", link: "/前端基础/Css/css踩坑记录" },
    { text: "Vue", link: "/Vue/原理剖析/diff算法优化 一" },
    // ...
  ],
  sidebar: {
    "/前端基础/": [
      { text: "Css", collapsed: true, items: [...] },
      // ...
    ],
    // ...
  }
};
```

### 2. countFiles.mjs - 文件统计与首页更新

**作用**：统计文档数量，更新首页的文章统计。

**工作流程**：

```javascript
// 步骤1: 递归统计 .md 文件数量
countFiles("./docs")
  ↓
  遍历所有文件和目录
  ↓
  统计 .md 文件总数

// 步骤2: 读取现有首页内容
readFile("./docs/index.md")
  ↓
  保留用户自定义的配置（hero、features 等）

// 步骤3: 智能更新文章数量
replace(/(details:\s*)(文章数量\s*)\d+/, `$1$2${articleCount}`)
  ↓
  如果找不到"文章数量"，在第一个 feature 中添加

// 步骤4: 写入更新后的首页
writeFile("./docs/index.md", finalContent)
```

**关键改进**：

1. **保留用户自定义内容**
   ```javascript
   // 读取现有内容，而不是完全覆盖
   const existingContent = await readFile("./docs/index.md");
   
   // 只更新文章数量部分
   const updatedContent = existingContent.replace(
     /(details:\s*)(文章数量\s*)\d+/,
     `$1$2${articleCount}`
   );
   ```

2. **智能匹配**
   - 使用正则表达式精确匹配文章数量
   - 如果找不到，自动添加到第一个 feature

**输出示例**：
```yaml
# docs/index.md
features:
  - icon: 🎨
    title: 前端基础
    details: 深入理解 CSS、JavaScript 等前端基础知识
  - icon: ⚡
    title: Vue 原理剖析
    details: 文章数量 32  # ← 自动更新这里
```

### 3. watch.mjs - 文件监听器

**作用**：实时监听 `docs` 目录变化，自动触发更新。

**工作流程**：

```javascript
// 步骤1: 初始化文件监听器
chokidar.watch("./docs/**/*.md", {
  ignored: /node_modules/,
  persistent: true,
  ignoreInitial: true  // 启动时不触发事件
})

// 步骤2: 监听文件变化事件
watcher
  .on("add", path => debounceUpdate())      // 新增文件
  .on("change", path => debounceUpdate())   // 修改文件
  .on("unlink", path => debounceUpdate())  // 删除文件
  .on("addDir", path => debounceUpdate())  // 新增目录
  .on("unlinkDir", path => debounceUpdate()) // 删除目录

// 步骤3: 防抖处理
debounceUpdate()
  ↓
  500ms 内多次变化只执行一次
  ↓
  执行 pnpm updateNav
```

**关键技术点**：

1. **防抖（Debounce）**
   ```javascript
   let updateTimer = null;
   
   function debounceUpdate() {
     if (updateTimer) {
       clearTimeout(updateTimer);  // 清除之前的定时器
     }
     
     updateTimer = setTimeout(async () => {
       await execAsync("pnpm updateNav");
     }, 500);  // 500ms 后执行
   }
   ```
   - 避免频繁触发更新
   - 短时间内多次变化只执行一次

2. **异步执行**
   ```javascript
   const execAsync = promisify(exec);
   await execAsync("pnpm updateNav");
   ```
   - 使用 Promise 包装 exec
   - 等待更新完成再继续

3. **优雅退出**
   ```javascript
   process.on("SIGINT", () => {
     watcher.close();
     process.exit(0);
   });
   ```
   - 监听 Ctrl+C
   - 关闭监听器后退出

### 4. setup-hooks.mjs - Git Hooks 安装器

**作用**：自动安装 Git pre-commit hook，在提交前自动更新导航。

**工作流程**：

```javascript
// 步骤1: 确定路径
const rootDir = path.resolve(__dirname, "..");
const hooksDir = path.join(rootDir, ".git", "hooks");

// 步骤2: 生成 hook 脚本内容
const preCommitHook = `#!/bin/sh
cd "${rootDir}"
pnpm updateNav
git add docs/index.md config.js
`;

// 步骤3: 写入 .git/hooks/pre-commit
fs.writeFileSync(preCommitPath, preCommitHook, "utf8");

// 步骤4: 添加执行权限（Unix 系统）
chmod +x pre-commit
```

**生成的 hook 脚本**：
```bash
#!/bin/sh
# 自动更新导航和文件统计
cd "/path/to/blogs"
pnpm updateNav

# 将更新后的文件添加到暂存区
git add docs/index.md config.js
```

**工作原理**：
- Git 在执行 `git commit` 前会自动运行 `.git/hooks/pre-commit`
- Hook 执行 `pnpm updateNav` 更新配置
- 将更新后的文件添加到暂存区
- 如果 hook 成功，继续提交；失败则阻止提交

## 三种自动化方式

### 方式1: Git Pre-commit Hook

**触发时机**：`git commit` 执行前

**实现机制**：
```
用户执行: git commit -m "添加文章"
    ↓
Git 检查: .git/hooks/pre-commit 是否存在？
    ↓
执行 hook: pnpm updateNav
    ↓
更新文件: config.js, docs/index.md
    ↓
添加到暂存区: git add docs/index.md config.js
    ↓
继续提交: git commit
```

**优点**：
- ✅ 提交时自动更新，无需记忆
- ✅ 确保提交的配置是最新的
- ✅ 适合团队协作

**安装**：
```bash
pnpm setup:hooks
# 或首次安装依赖时自动执行（postinstall）
```

### 方式2: npm Scripts 生命周期钩子

**触发时机**：执行 `pnpm dev` 或 `pnpm build` 前

**实现机制**：
```json
{
  "scripts": {
    "predev": "pnpm updateNav",   // dev 前执行
    "dev": "vitepress dev",
    "prebuild": "pnpm updateNav", // build 前执行
    "build": "vitepress build"
  }
}
```

**工作流程**：
```
用户执行: pnpm dev
    ↓
npm 自动执行: predev
    ↓
执行: pnpm updateNav
    ↓
更新配置
    ↓
继续执行: vitepress dev
```

**优点**：
- ✅ 开发/构建时自动更新
- ✅ 无需额外操作
- ✅ 使用 npm 内置机制

### 方式3: 文件监听模式

**触发时机**：`docs` 目录下的文件发生变化时

**实现机制**：
```
启动监听: pnpm watch:docs
    ↓
chokidar 监听: ./docs/**/*.md
    ↓
文件变化事件触发
    ↓
防抖处理（500ms）
    ↓
执行: pnpm updateNav
    ↓
继续监听...
```

**优点**：
- ✅ 实时更新，无需手动操作
- ✅ 适合频繁修改文档的场景
- ✅ 可以独立运行

**使用场景**：
```bash
# 终端1: 开发服务器
pnpm dev

# 终端2: 文件监听
pnpm watch:docs

# 然后编辑文档，配置会自动更新
```

## 完整执行流程示例

### 场景：添加新文章

**方式1: Git Hook（推荐）**
```
1. 创建文件: docs/Vue/新文章.md
2. git add docs/Vue/新文章.md
3. git commit -m "添加新文章"
   ↓
   [自动执行]
   ├─ pre-commit hook 触发
   ├─ pnpm updateNav
   │  ├─ generate.cjs → 更新 config.js
   │  └─ countFiles.mjs → 更新 docs/index.md
   ├─ git add docs/index.md config.js
   └─ 继续提交
4. ✅ 提交完成，配置已更新
```

**方式2: 开发时监听**
```
1. 启动监听: pnpm watch:docs
2. 创建文件: docs/Vue/新文章.md
   ↓
   [自动执行]
   ├─ chokidar 检测到文件变化
   ├─ 防抖等待 500ms
   ├─ pnpm updateNav
   │  ├─ generate.cjs → 更新 config.js
   │  └─ countFiles.mjs → 更新 docs/index.md
   └─ ✅ 配置已更新
```

**方式3: 构建前更新**
```
1. 创建文件: docs/Vue/新文章.md
2. pnpm build
   ↓
   [自动执行]
   ├─ prebuild 钩子触发
   ├─ pnpm updateNav
   │  ├─ generate.cjs → 更新 config.js
   │  └─ countFiles.mjs → 更新 docs/index.md
   └─ vitepress build
3. ✅ 构建完成，配置已更新
```

## 关键技术点总结

### 1. npm Scripts 生命周期
- `pre*`: 在对应脚本前执行
- `post*`: 在对应脚本后执行
- 例如：`predev` 会在 `dev` 前自动执行

### 2. Git Hooks
- `.git/hooks/pre-commit`: 提交前执行
- 必须是可执行文件（Unix: `chmod +x`）
- Hook 返回非 0 会阻止提交

### 3. 文件监听
- 使用 `chokidar` 库监听文件系统变化
- 支持递归监听、忽略规则
- 需要防抖避免频繁触发

### 4. 智能更新
- 读取现有内容，只更新需要更新的部分
- 使用正则表达式精确匹配
- 保留用户自定义配置

## 依赖关系

```
package.json
├─ scripts
│  ├─ updateNav → generate.cjs + countFiles.mjs
│  ├─ predev → updateNav
│  ├─ prebuild → updateNav
│  ├─ watch:docs → scripts/watch.mjs
│  └─ setup:hooks → scripts/setup-hooks.mjs
│
├─ devDependencies
│  ├─ vitepress (构建工具)
│  └─ chokidar (文件监听)
│
└─ postinstall → setup-hooks.mjs (首次安装时设置 Git hooks)
```

## 使用建议

1. **首次设置**：运行 `pnpm setup:hooks`（或依赖安装时自动执行）
2. **日常开发**：直接提交，Git hook 会自动更新
3. **频繁修改**：可运行 `pnpm watch:docs` 实时更新
4. **手动更新**：仍可使用 `pnpm updateNav`

这样的设计既保证了自动化，又保留了灵活性。

